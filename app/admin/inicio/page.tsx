export const dynamic = "force-dynamic";

import InicioClient from "./InicioClient";

export const metadata = {
  title: "Editar Inicio | Admin Zumomix",
};

export default function InicioPage() {
  return <InicioClient />;
}
